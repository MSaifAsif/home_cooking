'use strict';

//Setting up route
angular.module('category').config(['$stateProvider',
    function ($stateProvider) {
        // Category state routing
        $stateProvider
            .state('category', {
                // no routes
            });
    }
]);